apiVersion: v1
kind: Secret
metadata:
  name: grafana-secret
  namespace: microservices
type: Opaque
data:
  # SECURITY: Use external secret management in production
  # Example: kubectl create secret generic grafana-secret --from-literal=admin-password=YOUR_PASSWORD
  admin-password: ""  # Set via external secret management