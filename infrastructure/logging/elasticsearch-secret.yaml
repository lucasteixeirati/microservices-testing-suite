apiVersion: v1
kind: Secret
metadata:
  name: elasticsearch-secret
  namespace: microservices
type: Opaque
data:
  # SECURITY: Use external secret management in production
  # Example: kubectl create secret generic elasticsearch-secret --from-literal=password=YOUR_PASSWORD
  password: ""  # Set via external secret management